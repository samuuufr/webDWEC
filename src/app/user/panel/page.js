import CryptoDashboard from '@/app/components/CryptoDashboard';

export default function Dashboard(){
    return(
        <div className="min-h-screen bg-gradient-to-br from-gray-900 via-blue-900 to-purple-900">
            <CryptoDashboard />
        </div>
    );
}